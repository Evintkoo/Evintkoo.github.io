---
title: "Aegis"
tagline: "An authorized web pentest toolkit in Rust: one static binary covers black-box DAST across the OWASP Top 10, local source-code SAST, real-CVE matching, and out-of-band collaboration, detection-only and rate-limited by default."
ogDescription: "Rust web pentest toolkit: 40 DAST checks across the OWASP Top 10, tree-sitter SAST rules, CVE matching, and OOB collaboration — detection-only, dry-run by default."
tags: ["Open Source", "Security", "Rust"]
topic: "infra"
tech: ["Rust", "Tree-sitter", "OSV.dev / NVD", "Async HTTP"]
repo: "https://github.com/Evintkoo/aegis"
featured: false
order: 12
breakdown:
  center: "Aegis"
  centerDescription: "A single Rust binary covering black-box DAST, local-source SAST, real-CVE matching, and out-of-band collaboration."
  branches:
    - label: "DAST — Live Site Scanning"
      description: "Network-facing checks that crawl a live target, fuzz it, and turn hits into verified findings."
      repoPath: "crates/dast"
      nodes:
        - title: "Attack-surface discovery"
          description: "Crawls same-origin links and forms, parses robots.txt/sitemap.xml, mines JS bundles for API endpoints, and Arjun-style brute-forces hidden parameter names before fuzzing."
          repoPath: "crates/dast/src/discovery.rs"
        - title: "40 OWASP-mapped vulnerability checks"
          description: "Covers injection (SQLi, NoSQLi, cmdi, SSTI, LDAP/XPath, log4shell), XSS/CSRF/SSRF/IDOR/JWT/GraphQL and more, each finding tagged with a WSTG v4.2 test ID, CWE, and CAPEC ref."
          repoPath: "crates/dast/src/checks"
        - title: "Verification & proof pass"
          description: "Grades every finding confirmed/firm/tentative, extracts real proof (DB version via error-based SQLi, an /etc/passwd line for LFI, evaluated SSTI expressions), and emits a replayable curl PoC."
          repoPath: "crates/dast/src/verify.rs"
        - title: "Out-of-band collaborator listener"
          description: "A standalone collaborator binary that catches blind SSRF/stored-XSS callbacks over raw TCP, buckets hits by token, and persists them to a JSONL log."
          repoPath: "crates/collaborator"
    - label: "SAST — Static Source Analysis"
      description: "Offline scanning of a local source tree via --src, independent of any live target."
      repoPath: "crates/sast"
      nodes:
        - title: "Tree-sitter rule engine"
          description: "11 language-aware queries (SQLi string-concat, command exec/eval, unsafe deserialization, weak crypto, path traversal, SSRF, open redirect, XSS sinks, disabled TLS verification, XPath/LDAP injection, weak PRNGs) across Rust/TS/TSX/JS/Python."
          repoPath: "crates/sast/src/query_rules.rs"
        - title: "Hardcoded-secret scanner"
          description: "Regex-based secret detection that lossily decodes non-UTF-8 files so a single invalid byte can't silently disable the rule for the whole file."
          repoPath: "crates/sast/src/secrets.rs"
        - title: "Self-scan feedback-loop guard"
          description: "Canonicalized-path exclusion so re-running a scan over a tree containing this tool's own prior CVE-record output doesn't re-detect its own evidence text as new secrets."
          repoPath: "crates/sast/src/walker.rs"
    - label: "CVE Intelligence & Reporting"
      description: "Turns findings into real, referenceable vulnerability records instead of ad-hoc log lines."
      repoPath: "crates/cve-lookup"
      nodes:
        - title: "Real-CVE matching (OSV/NVD)"
          description: "Fingerprints component@version from server banners and queries OSV.dev plus optional NVD to attach genuine, matched CVEs instead of guesses."
          repoPath: "crates/cve-lookup/src/lookup.rs"
        - title: "CVE-record writer"
          description: "Persists every finding as a cvelistV5-shaped JSON record, giving self-discovered issues honest PENTEST-LOCAL IDs rather than fabricating a real-looking CVE number."
          repoPath: "crates/core/src/cve/writer.rs"
        - title: "Standards-mapped reporting"
          description: "Every finding carries WSTG/CWE/CAPEC refs plus JSON/HTML report output and stable exit codes built for CI gating and agent/script consumption."
          repoPath: "crates/core/src/standards.rs"
---

import ProjSectionHeading from '../../components/ProjSectionHeading.astro';

<ProjSectionHeading label="Overview">What is it?</ProjSectionHeading>

Aegis is a web application security scanner for systems you own or are explicitly authorized to test. It ships as a single `pentest` binary with no Python and no runtime dependencies — `cargo build` produces two static binaries, detection-only and dry-run by default.

<ProjSectionHeading label="Coverage">What it checks</ProjSectionHeading>

- **30 DAST modules** — spanning the OWASP Top 10: SQLi, NoSQLi, command injection, SSTI, path traversal, XXE, SSRF, IDOR, JWT weaknesses, CORS misconfiguration, and more.
- **6 SAST rules** — tree-sitter rules for SQLi concatenation, command exec/eval, unsafe deserialization, weak crypto, path traversal, and hardcoded secrets across Rust/TS/JS/Python.
- **CVE matching** — cross-references findings against OSV.dev and NVD, with full CVE record fetch.
- **Out-of-band collaboration** — a companion `collaborator` binary listens for blind SSRF and stored-XSS callbacks.

<ProjSectionHeading label="Design">How it works</ProjSectionHeading>

Everything runs through one CLI: a dry run prints the plan without sending anything, `--confirm` executes it. It crawls links and forms automatically and fuzzes every parameter it finds, or targets a single parameter with `-p`. Static analysis (`--src`) runs standalone against a local source tree, no live target required.
